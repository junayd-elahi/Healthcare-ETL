healthcare_dbt:
  outputs:
    dev:
      type: snowflake
      account: ITMOOCQ-WR99945
      user: JUNAYDELAHI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: TRANSFORMER
      database: HEALTHCARE
      warehouse: ELT_WH
      schema: DBT_JUNAYD
      threads: 4

    ci:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT', '') }}"
      user: "{{ env_var('SNOWFLAKE_USER', '') }}"
      private_key: "{{ env_var('SNOWFLAKE_PRIVATE_KEY', '') }}"
      role: TRANSFORMER
      database: HEALTHCARE
      warehouse: ELT_WH
      schema: CI
      threads: 4

  target: dev